import React from 'react';
import {View} from 'react-native';
import {ScreenProps} from '../../../../App';

const StudyTimer: React.FC<ScreenProps> = ({navigation}) => {
  return <View style={{flex: 1}}></View>;
};

export default StudyTimer;
